// Segment layout for /dashboard routes; keep it minimal and delegate html/body to root.
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
